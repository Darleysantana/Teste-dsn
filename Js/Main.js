/*
var pagina = 1;
var produto = 'https://frontend-intern-challenge-api.iurykrieger.now.sh/products?page='
const Button = getElement('.button'),
      container = getElement('.imagem'),
      erroMessage = getElement('.error');

var nome, // Nome ou numero passado na caixa de busca
    imagem, // Responsavel por guardar os dados recebidos da API
    descricao; // Responsavel por receber o HTML

    function requestInfo(url, name) {
        fetch(url + name)
          .then(response => response.json())
          .then(data => {
            imagem = data;
          })
          .catch(err => console.log(err));
      }
      requestInfo(produto, nome);
      
      searchButton.addEventListener('click', event => {
       produto = produto + pagina;
});
      */
     